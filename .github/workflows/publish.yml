name: Publish

on:
  release:
    types: [published]

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          registry-url: 'https://registry.npmjs.org'

      - name: Upgrade npm (for trusted publishing OIDC)
        run: npm install -g npm@latest

      - name: Print versions
        run: |
          node --version
          npm --version

      - run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build WASM via Docker
        uses: docker/build-push-action@v6
        with:
          context: ./wasm
          load: true
          tags: openslide-wasm-builder:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Extract WASM artifacts
        run: |
          docker create --name wasm-extract openslide-wasm-builder:latest
          mkdir -p wasm/dist
          docker cp wasm-extract:/build/out/openslide.js wasm/dist/
          docker cp wasm-extract:/build/out/openslide.wasm wasm/dist/
          docker rm wasm-extract

      - name: Build TypeScript
        run: npm run build:ts

      - name: Unit tests
        run: npm test

      # This workflow triggers on `release: published` and does not invoke
      # ci.yml, so without this step nothing on the path that actually
      # distributes binaries checks that licenses/ matches the sources they
      # were built from. It must stay ahead of `npm publish`: a mismatch has
      # to block publication, not be reported once the tarball is already on
      # the registry. Container name differs from ci.yml's so the two cannot
      # collide if both ever run on the same runner.
      - name: Verify committed licenses/ matches generated output
        run: |
          set -e
          docker create --name license-drift-publish openslide-wasm-builder:latest
          rm -rf /tmp/generated-licenses
          docker cp license-drift-publish:/build/out/licenses /tmp/generated-licenses
          docker rm license-drift-publish
          # licenses/README.md is hand-written and not generated; exclude it.
          if ! diff -rq --exclude=README.md licenses /tmp/generated-licenses; then
            echo "::error::Committed licenses/ does not match the build output — refusing to publish."
            echo "::error::Regenerate it: see licenses/README.md."
            exit 1
          fi
          echo "licenses/ is in sync with the build."

      - name: Publish to npm
        run: npm publish --provenance

      - name: Assemble release bundle
        run: |
          set -e
          mkdir -p release-bundle
          cp wasm/dist/openslide.js wasm/dist/openslide.wasm release-bundle/
          cp LICENSE NOTICE release-bundle/
          # Bundle the per-component license texts as one archive: attaching
          # them individually would flatten the directory structure and
          # collide on basename (multiple components ship a file named
          # COPYING, e.g. cairo, glib, gdk-pixbuf, fontconfig, pixman).
          tar czf release-bundle/licenses.tar.gz licenses
          # Complete corresponding source for the binaries above (LGPL-2.1 §6)
          git archive --format=tar.gz --prefix=openslide-js-src/ HEAD \
            -o release-bundle/openslide-js-source.tar.gz
          (cd release-bundle && find . -type f ! -name SHA256SUMS -exec sha256sum {} + > SHA256SUMS)
          ls -la release-bundle

      - name: Attach release bundle
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release-bundle/openslide.js
            release-bundle/openslide.wasm
            release-bundle/openslide-js-source.tar.gz
            release-bundle/licenses.tar.gz
            release-bundle/LICENSE
            release-bundle/NOTICE
            release-bundle/SHA256SUMS
